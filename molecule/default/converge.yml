---
- name: Converge
  hosts: all

  roles:
    - role: ../..

  post_tasks:
    - name: Check if camino_node service is active
      ansible.builtin.systemd:
        name: camino_node.service
        state: started
